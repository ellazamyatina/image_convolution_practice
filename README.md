## Image Convolution Practice

The implementation of image convolution in Python with support for various kernels, edge processing modes, and a performance comparison with classic libraries (OpenCV, Pillow...).
